<!--pages/searchRes/searchRes.wxml-->
<view>
  <view wx:for="{{ticket}}">
 <ticket from =  "{{item.from}}" to = "{{item.to}}" fromTime = "{{item.fromTime}}" bindtap="buy"></ticket>
  </view>
 
</view>
